#import("../src/abla-mvc.ab")

fun invalidAction(value: bool): MvcAction =
    replace("#result", mvcHtmlText("$value"))

fun main: int {
    val view = $mvcview <button onclick={invalidAction(true)}>Invalid</button>
    if (view.render() == "") 1 else 0
}
